server:
  port: '9001'

spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3307/service_user_db
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
db:
  url: jdbc:mysql://localhost:3307/indigo?useSSL=false&useTimezone=true&serverTimezone=UTC