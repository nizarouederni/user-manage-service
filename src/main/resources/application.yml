server:
  port: '9001'

spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3307/service_user_db
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect