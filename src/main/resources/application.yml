server:
  port: '9090'

spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/service_user_db
    password: admin
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update