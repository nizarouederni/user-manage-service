server:
  port: '9001'

spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3307/service_user_db
    password: admin
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update